#pragma once

//header, which includes all standart libraries

#include <iostream>  //std::cin, std::cout                                                          | Standart input/output library
//#include <string>    //std::string std::string.size()                                               | String container library
#include <memory>    //std::new std::delete                                                         | Memory managment library
#include <vector>    //std::vector, vector.push_back(), vector.begin(), vector.end(), vector,size() | Vector container library
//#include <map>       //std::map, map.size(), map.insert()                                           | Map container library
//#include <set>       //std::set, set.size()                                                         | Set container library
//#include <iterator>  //std::iterator                                                                | Iterator creation library
#include <thread>    //std::thread                                                                  | Multythread library
#include <mutex>     //std::mutex                                                                   | Mutex managment library
#include <time.h>    //std::clock(), std::clock_t, std::CLOCKS_PER_SEC                              | Internal clock library
//#include <algorithm> //std::for_each( begin(), end(), function)                                     | Algorithms library

//standart namespace
using namespace std;

